import React, { useEffect, useRef } from 'react';
              <Button
                type="button"
                variant="outline"
                onClick={handleGitHub}
                className="btn btn-medium"
              >
                Sign up with GitHub
              </Button>
